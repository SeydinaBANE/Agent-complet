name: CD

on:
  push:
    branches: [main]

jobs:
  push-images:
    name: Build & push Docker images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push agentcore
        uses: docker/build-push-action@v5
        with:
          context: ./agentcore
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/agentcore:latest
            ghcr.io/${{ github.repository }}/agentcore:${{ github.sha }}

      - name: Build and push flowrunner
        uses: docker/build-push-action@v5
        with:
          context: ./flowrunner/server
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/flowrunner:latest
            ghcr.io/${{ github.repository }}/flowrunner:${{ github.sha }}
